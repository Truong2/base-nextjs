import { FormLogin } from "~/components/modules/Authentication";

export const metadata = {
  title: "Source base - Login",
  description: "Source base - Login",
};

export default function LoginPage() {
  return <FormLogin />;
}
