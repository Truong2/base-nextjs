import { FormRegister } from "~/components/modules/Authentication";

export const metadata = {
  title: "Source base - Register",
  description: "Source base - Register",
};

export default function RegisterPage() {
  return <FormRegister />;
}
